import styled from 'styled-components';

export default styled.p`
  font-size: ${props => props.theme.font.fontSizeLg};
  font-weight: ${props => props.theme.font.fontWeightLight};
  margin: 1.6em 0;
`;
